# What do I want to report?
#       > Player with the least/most number of pings (flat, per match, per minute)
#       > Match with the lowest/highest total number of pings (flat, per minute)
#       > Team with the lowest/most number of pings (flat, per match, per minute)

# Thing I want to report eventually
#       > Player position with the lowest/most pings
#       > Region with the lowest/most pings


import PlayerInfo


def most_pings() -> PlayerInfo:
    return 0


class MatchAnalysis:

    def __init__(self, player_list: [PlayerInfo]):
        self.player_list = player_list

    def least_pings(self):
        return 0
